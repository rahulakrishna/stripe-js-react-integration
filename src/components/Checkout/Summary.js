import React from 'react';
import styled from 'styled-components';

const SummaryContainer = styled.div``;
const Heading = styled.h1``;

const Summary = () => (
  <SummaryContainer>
    <Heading>
      This form right here, is supposed to show the payment details
    </Heading>
  </SummaryContainer>
);

export default Summary;
